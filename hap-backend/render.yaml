services:
  - type: web
    name: hap-backend
    env: node
    plan: free
    rootDir: hap-backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: FRONTEND_URL
        value: https://hap-livid.vercel.app/
      - key: MONGO_PASSWORD
        value: SecurePassword123!
    # Database configuration (choose one)
    # For PostgreSQL (recommended)
    # - key: DATABASE_URL
    #   fromDatabase:
    #     name: hap-flashcard-db
    #     property: connectionString
    # For MongoDB (alternative)
    - key: MONGO_URI
      value: mongodb+srv://hetsondagar16_db_user:pHIcuRsSyQLxEFsm@hap-flashcards-cluster.nt4agov.mongodb.net/hap-flashcard?retryWrites=true&w=majority&appName=hap-flashcards-cluster

databases:
  - name: hap-flashcard-db
    plan: free
    databaseName: hap_flashcard
    user: hap_user
